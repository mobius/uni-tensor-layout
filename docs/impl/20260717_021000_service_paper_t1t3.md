# T1–T3 实施：服务硬化 + 论文基线 + 外部矩阵

> 文档时间: 2026-07-17 02:10:00  
> 版本: **1.7.0**  
> 计划: `docs/plan/20260716_042600_next_service_paper_hardening.md`

---

## 交付对照

### T1 服务可依赖

| 项 | 路径 / 行为 |
|----|-------------|
| 开停服 | `scripts/service_start.sh`, `service_stop.sh` |
| 排队等待 | serve result / metrics: `queue_wait_sec` |
| pin 策略 | job `pin_mode`: `grow` \| `strict`（`runtime/session.py`） |
| 本机回归 | `scripts/ci_device.sh`, `ci_all.sh` |
| 运维文档 | `docs/SERVICE.md` |

### T2 论文闭环

| 项 | 路径 |
|----|------|
| 全量 sweep | `python scripts/paper_sweep.py --exp-id esc4000_baseline --seed 0` |
| 出图 | `scripts/paper_plot.py artifacts/paper/<id>/` |
| 固化表 | `docs/impl/20260717_021000_paper_baseline_esc4000.md` |
| 决策叙事 | README「When to use Host / VE / serve」 |

### T3 外部数据 + Phi 按需

| 项 | 路径 |
|----|------|
| 稠密外部 | job `matrix_a` / `matrix_b`（`.npy`/`.npz`） |
| CSR 外部 | `csr_path` + 可选 `matrix_x` / `matrix_w` |
| 样例生成 | `scripts/make_sample_matrices.py` |
| 模板 | `jobs/dense_external.json`, `sparse_external.json` |
| Phi 列 | paper_sweep `--phi-try`；默认关；metrics `prep_sec`/`prep_note` |

**未做（可选遗留）**: serve 内 PhiWorker 长驻；recommend 分模式校准 band。

---

## 指标（无功耗）

- thr: `throughput_batches_per_sec`
- 延迟: `wall_sec`, serve `queue_wait_sec`
- 正确性: `max_abs_err` / status pass
- 可复现: `manifest.json` seed + version + devices
- 对照: `speedup_vs_oneshot`, `speedup_vs_host_dgemm`, `prep_*`

---

## 复现 checklist（第三人 ~30 min）

```bash
source env/.venv/bin/activate
export PYTHONPATH=src
export LD_LIBRARY_PATH=/opt/nec/ve/veos/lib64:$LD_LIBRARY_PATH
export VE_LD_LIBRARY_PATH=/opt/nec/ve/nlc/3.1.0/lib:/opt/nec/ve/nfort/5.4.1/lib:/opt/nec/ve/lib
export OMP_NUM_THREADS=48

bash scripts/ci_l0.sh
python scripts/make_sample_matrices.py
uct-run jobs/dense_external.json --host-only
uct-run jobs/sparse_external.json --host-only

bash scripts/service_start.sh
uct-run --socket jobs/service_dense_stream.json
python scripts/submit_loop.py --job jobs/service_dense_stream.json --n 5 --socket
bash scripts/service_stop.sh

python scripts/paper_sweep.py --exp-id esc4000_baseline --seed 0
python scripts/paper_plot.py artifacts/paper/esc4000_baseline   # optional viz
```

---

## 方法附录（1 页）

- **Job schema**: JSON `type` + sizes / external paths / `phi` / `force_ve` / `pin_mode` — 见 `docs/SERVICE.md` 与 `jobs/*.json`。
- **Serve**: Unix socket 串行 job → 共享 AVEO pin session；见 architecture serve notes + SERVICE。
- **Claim**: 常驻 pin thr ≫ cold oneshot；中尺寸单次 GEMM 常由 Host OpenBLAS 胜出。
