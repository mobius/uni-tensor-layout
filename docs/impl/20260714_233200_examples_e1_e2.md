# X1 实施：终端案例 E1 + E2

> 文档时间: 2026-07-14 23:32:00  
> 计划: `docs/plan/20260714_232700_terminal_examples_roadmap.md`  
> 状态: **X1 完成**

---

## 交付

| 项 | 路径 |
|----|------|
| 公共骨架 | `examples/_common.py` |
| 案例索引 | `examples/README.md` |
| **E1** 批处理稠密回归 | `examples/e1_batch_dense_regression.py` |
| **E2** 稀疏 stencil → 稠密 | `examples/e2_sparse_then_dense.py` |
| stencil CSR | `apps/spmv_dataprep.stencil5_csr` |
| L0 smoke | `tests/test_examples_smoke.py` |

默认：**Phi 关**（`--phi` 打开）；`--host-only` 可降级。

---

## 实机结果（本机）

### E1（3 batch，512×384×256）

| 项 | 值 |
|----|-----|
| status | pass |
| gemm | VE_AVEO / col_blocks / ve1 |
| thr | ~3.9 b/s |
| vs fixed 3-VE（单 batch 对照） | auto 更快（~1.7× 量级，视批次） |
| vs Host numpy | Host 仍更快（预期） |

### E2（stencil 40×40，nrhs=96，n_out=128）

| 项 | 值 |
|----|-----|
| status | pass |
| nnz | 7840 |
| phases | spmv ~0.02s，gemm ~0.19s |
| vs Host e2e | Host 更快（预期） |

产物：`artifacts/examples/e1_*` / `e2_*`（metrics.json、timeline.jsonl、plan.json）。

---

## 复现

```bash
source env/.venv/bin/activate
export PYTHONPATH=src
export LD_LIBRARY_PATH=/opt/nec/ve/veos/lib64:$LD_LIBRARY_PATH
export VE_LD_LIBRARY_PATH=/opt/nec/ve/nlc/3.1.0/lib:/opt/nec/ve/nfort/5.4.1/lib:/opt/nec/ve/lib

pytest -q tests/test_examples_smoke.py
python examples/e1_batch_dense_regression.py
python examples/e2_sparse_then_dense.py
```

---

## 下一步（X2）

E3 dataprep 投影、E4 job DAG、E5 sustained CLI。
