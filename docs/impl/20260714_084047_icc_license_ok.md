# 实现记录 — 更新 license 后 ICC 恢复

> 文档时间: 2026-07-14 08:40:47  
> License: `~/parallel_studio.lic`（**不入库**）

---

## 探测

| 项 | 结果 |
|----|------|
| 文件更新 | 736 bytes（此前 724） |
| chklic feature | **含 Comp-CL**（及 CCompL 等） |
| `try_icc_license()` | **ok=True** |
| 编译器标签 | **icc-mmic** |

## 测试

| 用例 | 结果 |
|------|------|
| ICC 探针 | pass |
| `compile_phi_dgemm(force=True)` | `dgemm_rect.mic` via **icc -mmic** |
| Phi DGEMM 64/256/512 | pass，err ≤ 2e-13 |
| pytest `test_device_real` | 5 passed |
| 全套 `run_real_tests.py` | **failed=0** |
| multi-VE NLC | pass |
| Phi peak smoke | ~570 GFLOPS |

## 复现

```bash
export INTEL_LICENSE_FILE=$HOME/parallel_studio.lic
source env/.venv/bin/activate
python -c "from uni_cute_tensor.backends.phi_dgemm import try_icc_license; print(try_icc_license()['ok'])"
python scripts/run_real_tests.py --phi-m 256 --phi-k 256 --phi-n 256
```

## 备注

- 朴素 pthread DGEMM 性能仍低（正确性优先）；ICC 路径已恢复，后续可加 IMCI/OpenMP 优化。  
- 流程仍与 intel_phi 文档一致：license 文件 + compilervars + `icc -mmic`。  
